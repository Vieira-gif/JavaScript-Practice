const num1 = 9

/*
console.log(100/ 0) // Infinity | ao ser checado retorna True

console.log(num1 ** 0.5) // <- Raiz quadrada

console.log(`
${Math.floor(num1)} // <- Arredonda o numero para baixo
==========================================`) 

console.log(`
${Math.ceil(num1)} // <- Arredonda o numero para cima
==========================================`) 


console.log(`
${Math.round(num1)} // <- Arroedonda para o numero mais proximo
==========================================`) 
//[< 0.5 = 9 | 0.5 > = 10]

console.log(`
${Math.max(1, 2, 3, 4, 5, -10, -50, 1500, 9, 8, 7, 6)} // <- Acha o maior numero
==========================================`) 

console.log(`
${Math.min(1, 2, 3, 4, 5, -10, -50, 1500, 9, 8, 7, 6)} // <- Acha o menor numero
==========================================`) 

console.log(`
${Math.random()} // <- Retorna um numero entre 1 e 0 (O numero 1 nunca aparece)
`)

const aleatorio = Math.round(Math.random() * (10 - 5) + 5)

console.log(aleatorio)*/

