const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Júlia']

// array.splice(indice atural, delete, elem1, elem2...)

// const removidos = nomes.splice(-2, Number.MAX_VALUE)
// const adicionado = nomes.splice(-2, 0, 'Lucas')

// Pop

// const removidos = nomes.splice(-1, 1)

// Push

// const adicionados = nomes.splice(nomes.length, 0, 'Leonardo')

// Shift

// const removidos = nomes.splice(0, 1)

// Unshift

// const adicionados = nomes.nomes.splice(0, 0, 'Rafael', 'Donatelo')

console.log(nomes)
